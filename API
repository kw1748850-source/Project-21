erfect 👍 Let’s make it VERY SIMPLE.

No JWT ❌
No hashing ❌
No OAuth ❌
No database ❌

Just a simple login API using dictionary ✅

🚀 Step 1: Install
pip install fastapi uvicorn
🚀 Step 2: Create main.py
from fastapi import FastAPI, HTTPException

app = FastAPI()

# Dummy users (like fake database)
users = {
    "admin": "1234",
    "asees": "password"
}

# Simple Login API
@app.post("/login")
def login(username: str, password: str):
    if username in users and users[username] == password:
        return {"message": "Login successful"}
    else:
        raise HTTPException(status_code=401, detail="Invalid username or password")
🚀 Step 3: Run
uvicorn main:app --reload

Open browser:

http://127.0.0.1:8000/docs
🧪 Test Example

Try:

username: admin
password: 1234

✔ You get → "Login successful"

Wrong password → ❌ 401 error

That’s it.
This is the simplest possible login API in FastAPI.

If you want, I can also make:

✅ Signup + Login simple

✅ Login with JSON body instead of query

✅ Return user info after login

✅ Add token but still simple

Tell me what level you want 👌